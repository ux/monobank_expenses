<ul>
  <li><%= link_to 'Sidekiq', '/sidekiq' %></li>
  <li><%= link_to 'Mortgage calculator', routes.mortgage_path %></li>
</ul>

<%= link_to 'Add user', routes.new_user_path %>

<ul>
  <% users.each do |user| %>
    <li>
      <%= link_to user.name, routes.user_path(user.client_id) %>
    </li>
  <% end %>
</ul>
